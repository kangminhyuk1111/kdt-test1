<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      * {
        margin: 0;
        padding: 0;
      }
      body {
        background-color: gray;
      }

      img {
        width: 40%;
        margin: 0 auto;
        padding-bottom: 10px;
      }
      .container {
        width: 30%;
        height: 50%;
        background-color: aquamarine;
        border-radius: 5rem;
        text-align: center;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        display: flex;
        flex-direction: column;
        justify-content: center;
        background-color: white;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <img alt="..." />
      <div><b><%= userId%>님 환영합니다 !</b></div>
      <div>비밀번호 : <%= userPw%></div>
      <div>이름 : <%= userName%></div>
      <div>나이 : <%= userAge%></div>
    </div>

    <script>
      const img = document.querySelector("img");
      const src = "<%= userImg %>";

      console.log(src);

      img.setAttribute("src", `../uploads/${src}`);
    </script>
  </body>
</html>
